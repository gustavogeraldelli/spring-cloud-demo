spring:
  application:
    name: eurekaserver

  security:
    user:
      name: ${ADMIN_USERNAME}
      password: ${ADMIN_PASSWORD}

server:
  port: 8761

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false